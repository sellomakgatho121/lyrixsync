import type { CapacitorConfig } from '@capacitor/cli';

/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: 'com.lyrixsync.app',
  appName: 'lyrixsync',
  webDir: 'out',
  bundledWebRuntime: false,
};

export default config;
const config: CapacitorConfig = {
  appId: 'com.lyrixsync.app',
  appName: 'lyrixsync',
  webDir: 'out'
};

export default config;
